# RPi4(CPU 전용)에서 detect_video.py 실행에 필요한 최소 패키지
#
# 설치:
#   pip install -r requirements.txt
#
# 주의: RPi4는 aarch64(ARM64) 아키텍처라 PyPI 기본 wheel이 없을 수 있습니다.
#       torch 설치가 실패하면 CPU 전용 인덱스를 지정하세요:
#         pip install torch --index-url https://download.pytorch.org/whl/cpu
#       (라즈베리파이 OS 32bit면 aarch64 wheel이 없으므로 64bit OS 권장)
#
# GUI 없는 헤드리스 환경(SSH만 사용)이면 opencv-python 대신
# opencv-python-headless를 쓰는 편이 가볍고 의존성 충돌이 적습니다.
#   pip install opencv-python-headless
# 이 경우 detect_video.py는 --no-display 옵션으로 실행하세요.

torch>=2.0          # TorchScript 모델 로드/추론 (CPU 빌드)
opencv-python>=4.5  # 영상 입출력, letterbox 리사이즈, 박스 그리기
numpy>=1.21         # 전처리 배열 연산

# 학습(train_person_detector.py)까지 돌릴 때만 추가로 필요 — RPi4 추론에는 불필요
# pandas>=1.3
# albumentations>=1.3
# tqdm>=4.60
# matplotlib>=3.5
